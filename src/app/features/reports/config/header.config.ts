import { HeaderConfig } from '@core/types/header-types';

export const REPORT_HEADER_CONFIG: HeaderConfig = {
  title: 'Reportes',
  subtitle: 'Generar y descargar reportes del sistema',
  showActions: true,
  showFilters: true,
};
