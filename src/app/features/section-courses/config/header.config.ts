import { HeaderConfig } from '@core/types/header-types';

export const SECTION_COURSES_HEADER_CONFIG: HeaderConfig = {
  title: 'Cursos por Sección',
  subtitle: 'Asignación de cursos a secciones',
  showActions: true,
  showFilters: true,
};
