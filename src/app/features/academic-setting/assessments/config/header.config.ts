import { HeaderConfig } from '@core/types/header-types';

export const ASSESSMENT_HEADER_CONFIG: HeaderConfig = {
  title: 'Evaluaciones',
  subtitle: 'Gestión de evaluaciones y criterios de calificación',
  showFilters: true,
  showActions: true,
  showSelection: false,
};
