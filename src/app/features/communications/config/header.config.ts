import { HeaderConfig } from '@core/types/header-types';

export const COMMUNICATION_HEADER_CONFIG: HeaderConfig = {
  title: 'Comunicaciones',
  subtitle: 'Mensajes, notificaciones y anuncios',
  showActions: true,
  showFilters: true,
};
