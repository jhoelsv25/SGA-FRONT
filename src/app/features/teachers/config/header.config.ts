import { HeaderConfig } from '@core/types/header-types';

export const TEACHER_HEADER_CONFIG: HeaderConfig = {
  title: 'Docentes',
  subtitle: 'Gestionar docentes',
  showActions: true,
  showFilters: true,
};
