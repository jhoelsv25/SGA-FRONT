import { HeaderConfig } from '@core/types/header-types';

export const USER_HEADER_CONFIG: HeaderConfig = {
  title: 'Usuarios',
  subtitle: 'Gestión de usuarios del sistema',
  showActions: true,
  showFilters: true,
};
