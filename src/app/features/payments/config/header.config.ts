import { HeaderConfig } from '@core/types/header-types';

export const PAYMENT_HEADER_CONFIG: HeaderConfig = {
  title: 'Pagos',
  subtitle: 'Gestión de pagos y cuotas por estudiante',
  showActions: true,
  showFilters: true,
};
